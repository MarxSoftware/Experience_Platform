== Release == 
mvn release:prepare
mvn release:perform

set manager web.xml configuration to deployment

== update version == 
mvn versions:set -DnewVersion=5.2.1 -DprocessAllModules
mvn versions:commit -DprocessAllModules

== development ==
Sidebar styles: https://bootstrapious.com/p/admin-template
used color: sea

