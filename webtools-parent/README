== Release == 
mvn release:prepare
mvn release:perform

set manager web.xml configuration to deployment

== update version == 
mvn versions:set -DnewVersion=4.0.0-SNAPSHOT -DprocessAllModules
mvn versions:commit -DprocessAllModules

== development ==
Sidebar styles: https://bootstrapious.com/p/admin-template
used color: sea

