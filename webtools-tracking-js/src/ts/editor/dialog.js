import jsPanel from '../../../node_modules/jspanel4/dist/jspanel'

(function( dialog, document ) {
    
    dialog.open = function () {

    }

}( window.webtools.Dialog = window.webtools.Dialog || {}, document));